let productName = "Xe Dap", price = 10, quantity= 15;
const DISCOUNT_RATE = 0.1;
totalPrice = price * quantity * (1-DISCOUNT_RATE);

console.log("Sản phẩm: " + productName + ", Số lượng: " + quantity + ", Tổng giá: " + totalPrice);